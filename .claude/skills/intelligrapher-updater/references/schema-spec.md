# 数据格式标准

## 知识库必需章节
1. `## 常用图表类型`
2. `## 坐标轴规范`
3. `## 标注符号表`
4. `## 领域特殊要求`

## 审美库必需参数
1. `## 目标期刊列表`
2. `## 主色板（Primary）`：JSON，键 `primary`
3. `## 辅助色板（Secondary）`：JSON，键 `secondary`
4. `## 背景与网格建议`
5. `## 字体与字号`
6. `## 线型与标记规范`

## 校验规则
- 一级标题缺失：Error
- JSON 解析失败：Error
- 非法 HEX 色值：Warning
